version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - JWT_SECRET=${JWT_SECRET:-change_this_secret_key_in_production}
      - FRONTEND_URL=http://localhost:5173
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-your_anthropic_api_key_here}
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/marketplace.db:/app/marketplace.db
    restart: unless-stopped
    networks:
      - marketplace-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - marketplace-network

networks:
  marketplace-network:
    driver: bridge
